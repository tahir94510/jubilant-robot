<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Post-splash window background in dark mode (NormalTheme): matches
         AppThemes.dark surface so the native-splash -> first-Flutter-frame
         handoff never flashes. NOTE: the SPLASH field itself is now the light
         "paper" cream in EVERY mode (see @color/splash_background) so the
         self-contained cream badge merges seamlessly; this dark surface only
         paints behind the app AFTER the splash dismisses. -->
    <color name="app_surface">#161512</color>
</resources>
