<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+ system splash, dark mode. @color/splash_background resolves to
     the dark app surface (values-night) and @drawable/splash_icon to the LIGHT
     "paper" Q (drawable-night), so the badge is BOXLESS and theme-adaptive: a
     light Q on a dark field, never a cream tile. windowBackground (post-splash)
     keeps the dark app surface so the handoff to a dark-themed app does not
     flash. -->
<resources>
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@color/app_surface</item>
    </style>
</resources>
