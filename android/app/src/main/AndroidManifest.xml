<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Reklamlar (AdMob) ve Play Faturalandirma icin ag erisimi -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Dokunsal geri bildirim (titresim): HapticFeedback'in Android'de
         gercekten titresmesi icin VIBRATE gerekir; olmadan sessizce no-op. -->
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- Android 13+ gunluk hatirlatma bildirimi izni (uygulama icinden istenir) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Cihaz yeniden basildiginda gunluk hatirlatmayi yeniden kurmak icin
         (INEXACT alarmlar da reboot'ta dusuyor; boot receiver yeniden kurar). -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Gunluk hatirlatma INEXACT alarm kullanir (inexactAllowWhileIdle):
         rahat bir oyun hatirlaticisi tam-zaman gerektirmez. Bu yuzden HER IKI
         exact-alarm iznini de (eklentinin birlesik manifeste enjekte ettigi
         dahil) KALDIRIYORUZ:
         - SCHEDULE_EXACT_ALARM kafa karistiran "Alarmlar ve hatirlaticilar"
           sistem ekranini aciyordu ve Play bunu "yalniz cekirdek-islev" izni
           olarak inceler; bir oyun hatirlaticisi bu esigi karsilamaz.
         - USE_EXACT_ALARM zaten yalniz alarm/takvim uygulamalarina ozel.
         OS bakim penceresinde birkac dakika sapma "bugun oyna" durtusu icin
         kabul edilebilir; HIGH onemli kanal yine de sesli/heads-up uyarir. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"
        tools:node="remove"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"
        tools:node="remove"/>
    <!-- AdMob reklam kimligi (Play Console "Veri guvenligi" formunda beyan edilir) -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>

    <application
        android:label="Quotecrack"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- AdMob uygulama kimligi; degeri android/app/build.gradle.kts icindeki
             manifestPlaceholders["admobAppId"] satirindan gelir. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${admobAppId}"/>

        <!-- GMA SDK'nin acilis init'ini erteleyip optimize eder: soguk
             baslangic ANR/cokme riskini dusurur (Google onerisi). -->
        <meta-data
            android:name="com.google.android.gms.ads.flag.OPTIMIZE_INITIALIZATION"
            android:value="true"/>
        <meta-data
            android:name="com.google.android.gms.ads.flag.OPTIMIZE_AD_LOADING"
            android:value="true"/>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- flutter_local_notifications, planlanmis bildirimler icin bu iki
             receiver'in UYGULAMA manifestinde bildirilmesini sart kosar
             (eklentinin kendi manifesti yalnizca izin tasir). Birincisi
             AlarmManager'in hedefi: o olmadan gunluk hatirlatma HIC calmaz.
             Ikincisi, cihaz yeniden basladiginda / uygulama guncellendiginde
             kurulu hatirlatmayi yeniden kurar (RECEIVE_BOOT_COMPLETED izni
             yukarida). -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />
        <!-- Bildirim aksiyonlari / bazi OEM'lerde teslim dayanikliligi icin. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver"
            android:exported="false" />
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
