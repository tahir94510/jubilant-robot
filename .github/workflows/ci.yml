# Quotecrack CI/CD
#
# Her push'ta: analiz + testler + indirilebilir debug APK + web onizleme
# (GitHub Pages). KEYSTORE_BASE64 secret'i tanimliysa Play Store'a
# yuklenecek IMZALI release AAB da uretilir (Actions > calisma > Artifacts).
#
# Kurulumunuz icin tek seferlik adimlar: docs/YAYINLAMA_REHBERI.md
name: CI

on:
  push:
    branches: [main, 'claude/**']
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze-test:
    name: Analyze & test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - run: flutter pub get
      - run: dart format --output=none --set-exit-if-changed lib test
      - run: flutter analyze
      - run: flutter test --reporter expanded

  check-secrets:
    name: Check signing secrets
    runs-on: ubuntu-latest
    outputs:
      has-keystore: ${{ steps.chk.outputs.has }}
    steps:
      - id: chk
        env:
          HAS: ${{ secrets.KEYSTORE_BASE64 != '' }}
        run: echo "has=$HAS" >> "$GITHUB_OUTPUT"

  build-apk-debug:
    name: Debug APK (telefonda deneme icin)
    runs-on: ubuntu-latest
    needs: analyze-test
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - run: flutter pub get
      - run: flutter build apk --debug
      - uses: actions/upload-artifact@v4
        with:
          name: quotecrack-debug-apk
          path: build/app/outputs/flutter-apk/app-debug.apk
          retention-days: 14

  build-aab-release:
    name: Signed release AAB (Play Store)
    runs-on: ubuntu-latest
    needs: [analyze-test, check-secrets]
    if: needs.check-secrets.outputs.has-keystore == 'true'
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - run: flutter pub get
      - name: Materialize signing config from secrets
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: |
          echo "$KEYSTORE_BASE64" | base64 -d > android/app/upload-keystore.jks
          {
            echo "storeFile=upload-keystore.jks"
            echo "storePassword=$KEYSTORE_PASSWORD"
            echo "keyAlias=$KEY_ALIAS"
            echo "keyPassword=$KEY_PASSWORD"
          } > android/key.properties
      - run: flutter build appbundle --release
      - uses: actions/upload-artifact@v4
        with:
          name: quotecrack-release-aab
          path: build/app/outputs/bundle/release/app-release.aab
          retention-days: 30

  build-web:
    name: Web preview build
    runs-on: ubuntu-latest
    needs: analyze-test
    permissions:
      pages: write
      contents: read
    steps:
      - uses: actions/checkout@v5
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - run: flutter pub get
      # --pwa-strategy=none: service worker olmasin ki onizleme asla
      # bayat (eski) surumde takili kalmasin.
      - run: >
          flutter build web --release
          --base-href "/jubilant-robot/app/"
          --pwa-strategy=none
      - name: Assemble Pages site (landing + privacy + app)
        run: |
          mkdir -p _site/app
          cp -r build/web/* _site/app/
          cp -r pages/* _site/
      - uses: actions/configure-pages@v5
        with:
          # Pages daha once acilmamissa ilk calismada otomatik acar.
          enablement: true
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy-pages:
    name: Deploy web preview
    runs-on: ubuntu-latest
    needs: build-web
    if: github.event_name == 'push'
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
